name: Build & Release DEB

on:
  push:
    tags:
      - "v*"

jobs:
  build-deb:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build deps
        run: |
          sudo apt update
          sudo apt install -y \
            debhelper \
            dh-python \
            python3-all \
            python3-pyqt5 \
            python3-psutil

      - name: Build deb
        run: |
          set -e
          dpkg-buildpackage -us -uc

          # ❗ ОБОВʼЯЗКОВО: копіюємо .deb у workspace
          mkdir -p dist
          cp ../xwallet-suite_*.deb dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: xwallet-deb
          path: dist/*.deb

  release:
    needs: build-deb
    runs-on: ubuntu-latest

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: xwallet-deb
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.deb
